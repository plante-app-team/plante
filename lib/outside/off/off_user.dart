/// Fields are intentionally invalid because all requests to OFF
/// are proxied by the Plante server anyways.
class OffUser {
  static const USERNAME = 'intentionally invalid';
  static const PASSWORD = 'intentionally invalid';
}
